# Short Response Questions 

1.Creating their onw branch,this is important to do because working on branch allows each member to develop features, fix bugs, or experiment with new ideas in an isolated environment without affecting the main codebase that others are using.

2. Developers use branches to not affect the main branch,code,and everything that each other team members could work on.

3.Git  is Distributed Version Control System,every developer has access to all changes,also wht developers use to take a snapshot of what the code looks like at the moment.Github is cloud based service that allows developers to store,manage and controls changes to their code.Git and Github are working together by combining local version control with cloud collaboration and hosting.

4.The person who did that command sequence switched git commit -m 'saving work' and git add .

the correct command sequence:
```
git add .
git commit -m "saving work"
git push 